<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Decision

<!-- lore:019fa488-2d21-73bb-9a91-950f23bc44bf -->
* **Switched from plan to build**: switched from plan to build.

### Gotcha

<!-- lore:019fa4a3-1aa0-7532-bcec-7597069ca2b5 -->
* **Cloudflare Email: binding email key vs REST API address**: Trap: Using \`address\` key (REST API convention) in a send\_email binding seems right because most email APIs use \`address\` — but Workers binding uses \`email\` key (EmailAddress type). Fix: Use \`email\` for binding, \`address\` only when calling the REST API directly. Similarly, attachment content differs: Workers binding accepts raw string or ArrayBuffer; REST API always requires base64-encoded string. Sending REST API-shaped payloads to a binding (or vice versa) will fail with type errors.

<!-- lore:019fa6a0-f96c-7ca1-9af2-4c9aa5b5e8d3 -->
* **Higgsfield 'IN THE DARK' preset bypass**: When generating videos with dark scenes (cave, darkness, night), Higgsfield's safety system automatically detects and applies an 'IN THE DARK' preset that restricts creative control. Trap: The preset appears as a helpful automatic optimization for dark content but actually constrains output quality. Fix: When the generation response includes a \`declined\_preset\` with a detected preset ID, include \`declined\_preset\_id\` in subsequent generation requests to bypass the preset entirely.

<!-- lore:019fa6c4-1b74-74a1-aa4f-e070bfe5c23d -->
* **static export CI fails if new components aren't git-tracked**: Trap: New component files (e.g., LiquidMetalHero.tsx, ui/ directory) exist locally and pass \`npm run build\`, so they seem safe. But if they aren't git-tracked, GitHub Pages CI fails with module-not-found errors — only git knows what to deploy. Fix: Always \`git add\` and commit new component files before pushing to main. Run \`git status\` to verify no untracked component files before deploy.

<!-- lore:019fa4bc-f333-7111-bba8-4e3e65068d5c -->
* **Static export form handler constraint**: Next.js static export (output: 'export') to GitHub Pages prohibits API routes. Form handlers must be external services like Cloudflare Workers. Cannot use API routes, server actions, or rewrite-based solutions. All form submissions must POST to an external endpoint. Trap: It looks natural to add an API route inside the Next.js project because modern frameworks encourage server endpoints, but \`output: 'export'\` disables the server runtime entirely. Fix: Create a separate Cloudflare Worker or use form services (Formspree, FormSubmit) as the endpoint.

### Pattern

<!-- lore:019fa48d-b416-7fd0-95ad-d527f6e88ed3 -->
* **jordanchristley-site dark emerald design system**: Dark emerald theme across JordanTreDaniel's site: Primary emerald #5cdb8a, Dark bg #050a07, Surface #2d8a4e, Gold accent #d4a843. Pages like emerald-tech use bg #07140d with green accents and radial gradients. Common patterns: rounded-3xl cards with border-emerald-200/15 and bg-emerald-950/40, uppercase tracking, max-w-6xl containers. ContactForm tone='emerald' uses panel bg-emerald-950/40, button bg-emerald-300 text-emerald-950, input border-emerald-300/20 bg-emerald-950/30.

<!-- lore:019fa48d-b414-72aa-9e90-a10f6d97ae54 -->
* **jordanchristley-site Next.js 16 static export to GitHub Pages**: JordanTreDaniel's personal site at /Users/jordanc/dev/jordanchristley-site uses Next.js 16 static export (output: 'export') deployed to GitHub Pages. Stack: framer-motion 12.42.2, react 19, tailwindcss 4, react-icons, Space\_Grotesk font (loaded in layout.tsx). ContactForm is a client component accepting title, description, cta, note, showCompany, and tone ('neutral' | 'emerald') props with styled toneStyles.

<!-- lore:019fa4bc-f336-7ec9-8d1e-693a2a119877 -->
* **Journey page scroll-driven story pattern**: The /emerald-tech page uses a scroll-driven journey with 3 scenes: 'The Overwhelm', 'The Gems' (interactive flash cards), 'The Door' (CTA). JourneyPage component implements scroll-scrub with sticky viewport, desktop nav bar, mobile hamburger with emerald icon, animated scene indicators, and AnimatePresence text transitions. Videos stored in \`public/scenes/\` as \`scene-NN.mp4\`, \`scene-NN-poster.png\`, \`scene-NN-mobile.mp4\`. Contact form at the end POSTs to a Cloudflare Worker. This pattern can be reused for other pages (e.g., Resource Realty by replacing the hero section).

### Preference

<!-- lore:019fa4a3-1a99-798f-81d8-f33fa62489f7 -->
* **Always include html and text versions for Cloudflare Email**: When sending emails via Cloudflare Email Service's send\_email binding, always include both html and text versions of the email body. Never send only html. This ensures deliverability across email clients that render plain text or reject html-only messages. User stated this as an unconditional rule.

<!-- lore:019fa6a0-f96e-72b6-9871-e6eec85e7ba2 -->
* **Higgsfield media generation budget discipline**: Prefer the cheapest viable tier for Higgsfield media generation until the visual approach is validated. Use Seedance 1.5 at 720p/4s for video, GPT Image 2 at medium quality for stills. Only upgrade by one tier (e.g., Seedance 2.0 mini) when confident in the direction. Always use \`--generate\_audio false\` to avoid wasting credits on audio.

<!-- lore:019fa4bc-f338-7a16-aa23-7d228e9284c9 -->
* **Prefer file comments over lore for specifics**: User explicitly stated: 'make sure not to take specific stuff that doesn't need to be known every time we work on agents.md and putting it there. could be comments to files, control your scope.' Implementation-level details (exact file paths, naming conventions, script bugs) belong in code comments, not in lore entries. Keep lore entries high-level and broadly applicable across the project. This is a directive about knowledge extraction scope.

<!-- lore:019fa6a0-f96f-7117-9b3a-ba6439b306a9 -->
* **Require spatial model before scene generation**: Never start generating scene images or videos without first defining a spatial model. The spatial model must include: camera path (perspective), exact color palette (hex values), light direction/quality, surface finish, background behavior, and explicit negative rules (no text, no people, no UI elements). Generating without a spatial model leads to miscoordinated scenes that fail to compose a coherent journey.

<!-- lore:019fa6c7-d5b2-7a73-8b9a-13d6fa21d8d0 -->
* **Sub-agents for implementation tasks**: When switching from plan to build mode, delegate implementation work to sub-agents using the Task tool. The user directed this approach for building the emerald-tech journey feature (July 27, 2026). Sub-agents handle parallel tasks (video pipeline, component customization, form wiring) while the main agent coordinates. This avoids explanatory suggestions and enables autonomous execution of build work.

<!-- lore:019fa4a3-1a9d-700d-8046-b21e06c4c836 -->
* **Trust official Cloudflare docs as source of truth**: For all Cloudflare Workers, Email Service, and related technologies, rely exclusively on official documentation (Cloudflare docs, REST API spec, @cloudflare/workers-types, Agents SDK repo) as the source of truth. Do not use third-party tutorials or assume conventions — verify every API shape, binding key, and behavior against the official spec. User stated 'always trust the original source' as an absolute rule.

<!-- lore:019fa4a3-1a9f-7feb-b188-2fd86cff5afd -->
* **Use npx wrangler types for generated Env interface**: Never define the Workers Env interface manually. Always run \`npx wrangler types\` to auto-generate the interface (with SendEmail, EmailAttachment, EmailAddress types) from wrangler.jsonc bindings and the workerd runtime. Manual definitions drift from actual bindings and miss type updates. User stated 'always use these generated types — do not define them manually' as an absolute rule, confirmed across 4+ sessions.

<!-- lore:019fa5ab-b183-79ad-ab41-5fed88490e8f -->
* **Video-flythrough scroll-driven site with optional 3D**: Prefers video-flythrough scroll-driven websites for portfolio/consulting pages. The user wants multiple stages/sections the user scrolls through (UX) so the viewer is not held prisoner by animations. 3D elements can be intermixed but secondary to video. Desktop nav bar, mobile hamburger with emerald icon. This pattern applies to jordanchristley-site emerald-tech page and similar consulting pages.
